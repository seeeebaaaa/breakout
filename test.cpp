// #include "util.h"
#include <iostream>
#include <vector>

// float func(float x) {
//   return log2f(x) / log2f(40);
//   // return x * (1.0f / 40);
// }

// template <typename T>
// void delete_element_in_vector(int i, std::vector<T> &a) {
//   typename std::vector<T>::iterator q;
//   q = a.begin();
//   q += i;
//   a.erase(q);
// }

int main() {
  // float a[2] = {0, 0}, b[2] = {4, 4}, p[2] = {1, 2};
  // float d = distance_between_line_and_point(a, b, p);
  // std::cout << "Distance: " << d << std::endl;

  // for (int i = 0; i < 10; i++) {
  //   int rad = (int)get_circle_radius(i, 10, 1, 20);
  //   std::cout << rad << std::endl;
  // }
  // for (int i = 1; i < 40; i++) {
  //   int current_color[3] = {0};
  //   // max colors: 256*2
  //   std::cout << (int)(2 * func(i)) << std::endl;
  //   switch ((int)(2 * func(i))) {
  //   case 0:
  //     current_color[0] = (int)((func(i)) * 256 * 2);
  //     current_color[1] = 255;
  //     break;
  //   case 1:
  //     current_color[0] = 255;
  //     current_color[1] = 255 - (int)((func(i) - 0.5) * 256 * 2);
  //     break;
  //   }
  //   std::cout << "Color: \e[38;2;" << current_color[0] << ";" << current_color[1] << ";" << current_color[2] << "m" << current_color[0] << ", " << current_color[1] << ", " << current_color[2] << "\e[0m" << std::endl;
  // }

  // int nr_of_places = 6;
  // int places[nr_of_places][2];
  // int width = 800, height = 1000;
  // for (int i = 0; i < nr_of_places; i++) {
  //   places[i][0] = width * (((i % 3) % 3) + 1) / 4;
  //   places[i][1] = height * (0.3 + (i / 3) * 0.4);
  //   std::cout << "[" << places[i][0] << "," << places[i][1] << "]" << std::endl;
  // }
  // std::vector<int> vec = {0, 1, 2, 3, 4};
  // for (int i : vec)
  //   std::cout << i << " ";
  // std::cout << std::endl;
  // delete_element_in_vector(2, vec);
  // for (int i : vec)
  //   std::cout << i << " ";
  // std::cout << std::endl;

  // std::vector<int> a = {123};
  // int &b = a[0];
  // int *p = &b;
  // a.push_back(9);
  // std::cout << a[0] << std::endl;
}
